"""Alert-domain models, ingestion, routing, and planning rules.

`inbox` provides the in-process alert receiver, queue, and local HTTP listener;
the remaining modules cover alert-source mapping, extraction, normalization,
and tool planning.
"""
